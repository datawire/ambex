dist: xenial
language: go
go:
  - "1.11.x"

addons:
  apt:
    sources:
      - sourceline: 'ppa:masterminds/glide'
    packages:
      - glide

install:
  - glide install

script:
  - ./smoketest.sh

notifications:
  email: false
